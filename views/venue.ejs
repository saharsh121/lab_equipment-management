<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lab Venue Details</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom Styles -->
    <style>
        body {
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container {
            margin-top: 50px;
            background: rgba(255, 255, 255, 0.95);
            color: #333;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            padding: 30px;
            width: 90%;
            max-width: 900px;
        }

        h1 {
            text-align: center;
            font-weight: 700;
            color: #333;
            margin-bottom: 20px;
        }

        /* Scrollable table container */
        .table-container {
            max-height: 400px; /* Adjust height here */
            overflow-y: auto;
            overflow-x: hidden;
            border-radius: 10px;
            border: 1px solid #ddd;
        }

        /* Sticky header for better UX */
        thead th {
            position: sticky;
            top: 0;
            background-color: #6a11cb !important;
            color: white;
            z-index: 10;
        }

        .table tbody tr:hover {
            background-color: #f1f1f1;
        }

        .btn-back {
            margin-top: 25px;
            background: #6a11cb;
            border: none;
            color: white;
            font-weight: 500;
            transition: 0.3s;
        }

        .btn-back:hover {
            background: #2575fc;
            transform: scale(1.05);
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>All Lab Venues</h1>

        <div class="table-container">
            <table class="table table-striped text-center align-middle mb-0">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Lab Name</th>
                        <th>Building Name</th>
                        <th>Floor</th>
                        <th>Room Number</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (venues.length > 0) { %>
                        <% venues.forEach(v => { %>
                            <tr>
                                <td><%= v.id %></td>
                                <td><%= v.lab_name %></td>
                                <td><%= v.building_name %></td>
                                <td><%= v.floor %></td>
                                <td><%= v.room_number %></td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="5">No venue records found.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="text-center">
            <button class="btn btn-back px-4 py-2 rounded" onclick="location.href='/student-home'">
                 Back to Dashboard
            </button>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
